import React from 'react'
import Listing from '../components/Listing'

function List({token}) {
    return (
        <div>
            <Listing token={token}/>
        </div>
    )
}

export default List
